//! {{description}}
//!
//! Library surface for [`{{full_name}}`]. Consumed by the stdio plugin
//! binary in `src/main.rs` and by contract tests in `tests/`.
//!
//! Modules:
//!
//! - [`backend`] — the [`animus_subject_protocol::SubjectBackend`] impl.
//! - [`config`] — env-driven runtime configuration.

#![warn(missing_docs)]

pub mod backend;
pub mod config;
